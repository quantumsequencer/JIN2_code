# 作業方針

目的は、必要な品質を保ちながら、無関係な読み込み・再調査・出力を減らすこと。
ユーザーの明示的な依頼を優先する。省コンテキストを理由に必要な調査や検証を省略しない。

## 読み込みと探索

- 毎回プロジェクト全体を列挙・通読しない。依頼で指定されたファイル・関数・エラーから着手する。IDEの開いているタブをすべて読む必要はない。
- 最初は対象ファイルの該当箇所と対応テストを読む。不明点が残る場合に限り、呼び出し元・依存先へ段階的に範囲を広げる。
- `rg -n` で対象ディレクトリと検索語を絞り、前後の必要な行だけ取得する。ファイル名だけ必要なら `rg --files` / `rg -l` を使う。
- `main.py` など大きなファイルは、まず関数名・クラス名・表示文言で検索する。全体構造が必要なときだけ全文を読む。
- 同じ会話で確認済みの内容は再利用する。変更・外部更新・記憶の不確かさがある箇所だけ再確認する。編集前には対象箇所の現状を確認する。
- 検索結果が大量になる場合は範囲を絞り直す。大量の出力を繰り返して文脈を埋めない。
- 配下の指示ファイルがある場合は対象に適用される指示を確認する。

## このプロジェクトの入口

- `test2/`：現在の開発対象。UIは `main.py`、起動・仕様・検証手順は `README.md` の関連節を参照。
- Python機能の調査は対象の `.py` と `test_*.py` から開始する。
- `test2/gateway_source/`：Gateway側の変更・調査が必要な場合に参照する。
- `SGMO2_original/`：メーカー配布物・原本。仕様確認や比較が必要な場合だけ該当資料を読む。通常の修正先は `test2/` とする。
- IDEに残った旧パスは存在を確認してから利用し、存在しないファイルの内容を推測しない。

## 通常の探索から外すもの

- `.venv/`、`__pycache__/`、`.stub_gateway/`、`bin/`、`obj/`、`Output/`、`External/`、`dlls/`。
- `reports/`、`analysis/`、`data/`、`logs/`、ログ・画像・PDF・実行ファイルなどの生成物や資料。
- 上記はアクセス禁止ではない。依頼や不具合に関係する場合は対象ファイルを明示して読む。
- CSV・計測ログは、まずヘッダー・必要な列・時刻範囲・少数行を確認する。大量データの集計はローカル処理し、結果と異常箇所だけ出力する。

## 変更と検証

- 依頼に必要な変更に集中し、無関係なリファクタリングや整形を混ぜない。
- 対象に対応する既存テストを先に実行する。例：`test2` を作業ディレクトリとして `.\.venv\Scripts\python.exe -B -m unittest test_gap_target -v`。
- 共通処理・通信・工程制御の変更や関連テストの失敗など、影響が広い場合は検証範囲も広げる。
- 変更も新しい懸念もなければ、成功済みの同じ検証を繰り返さない。文書のみの変更でアプリ全体を起動しない。
- テスト・Stub検証と実機検証を区別して報告する。既存の装置操作・停止・エラー処理の前提を保持する。

## 会話と引き継ぎ

- 日本語で簡潔に報告する。完了時は変更点・検証結果・残課題を中心にし、コードやログ全文を貼り直さない。
- 小さな作業で長い計画や繰り返しの説明を作らない。サブエージェントは明示的に依頼された場合だけ使う。
- 会話を移すための引き継ぎが必要な場合は、目的・対象パス・決定事項・検証済み事項・次の作業だけを短くまとめる。
- このファイルに作業履歴やコード全文を追記しない。入口や恒常的な手順が変わったときだけ更新する。
